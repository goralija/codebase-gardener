{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://codebase-gardener.dev/schemas/repository_automation_settings.schema.json",
  "type": "object",
  "required": ["schema_version", "repository", "baseline", "stats", "policy", "effective", "permissions", "recent_sessions", "recent_pr_plans"],
  "properties": {
    "schema_version": { "const": "1.0" },
    "repository": {
      "type": "object",
      "required": ["id", "full_name", "default_branch", "html_url"],
      "properties": {
        "id": { "type": "string" },
        "full_name": { "type": "string" },
        "default_branch": { "type": "string" },
        "html_url": { "type": "string" }
      },
      "additionalProperties": false
    },
    "baseline": {
      "type": "object",
      "required": ["analysis_id", "commit_sha", "source", "promoted_at"],
      "properties": {
        "analysis_id": { "type": ["string", "null"] },
        "commit_sha": { "type": ["string", "null"] },
        "source": { "type": ["string", "null"] },
        "promoted_at": { "type": ["string", "null"], "format": "date-time" }
      },
      "additionalProperties": false
    },
    "stats": {
      "type": "object",
      "required": ["report_count", "session_count", "completed_session_count", "pr_plan_count", "created_pr_count", "merged_pr_count", "blocked_pr_count", "latest_report_at"],
      "properties": {
        "report_count": { "type": "integer", "minimum": 0 },
        "session_count": { "type": "integer", "minimum": 0 },
        "completed_session_count": { "type": "integer", "minimum": 0 },
        "pr_plan_count": { "type": "integer", "minimum": 0 },
        "created_pr_count": { "type": "integer", "minimum": 0 },
        "merged_pr_count": { "type": "integer", "minimum": 0 },
        "blocked_pr_count": { "type": "integer", "minimum": 0 },
        "latest_report_at": { "type": ["string", "null"], "format": "date-time" }
      },
      "additionalProperties": false
    },
    "policy": {
      "type": "object",
      "required": ["id", "autonomy_mode", "manual_trigger_enabled", "scheduled_trigger_enabled", "commit_trigger_enabled", "risky_module_trigger_enabled", "pr_opened_trigger_enabled", "ci_failure_trigger_enabled", "commit_threshold", "created_at", "updated_at"],
      "properties": {
        "id": { "type": "string" },
        "autonomy_mode": {
          "type": "string",
          "enum": ["conservative", "assisted", "autonomous"]
        },
        "manual_trigger_enabled": { "type": "boolean" },
        "scheduled_trigger_enabled": { "type": "boolean" },
        "commit_trigger_enabled": { "type": "boolean" },
        "risky_module_trigger_enabled": { "type": "boolean" },
        "pr_opened_trigger_enabled": { "type": "boolean" },
        "ci_failure_trigger_enabled": { "type": "boolean" },
        "commit_threshold": { "type": "integer", "minimum": 1, "maximum": 500 },
        "created_at": { "type": "string", "format": "date-time" },
        "updated_at": { "type": "string", "format": "date-time" }
      },
      "additionalProperties": false
    },
    "effective": {
      "type": "object",
      "required": ["autonomous_pr_add_on_enabled", "can_create_autonomous_prs", "pr_creation_status", "default_commit_threshold", "confidence_threshold"],
      "properties": {
        "autonomous_pr_add_on_enabled": { "type": "boolean" },
        "can_create_autonomous_prs": { "type": "boolean" },
        "pr_creation_status": { "type": "string" },
        "default_commit_threshold": { "type": "integer", "minimum": 1 },
        "confidence_threshold": { "type": "number", "minimum": 0, "maximum": 1 }
      },
      "additionalProperties": false
    },
    "permissions": {
      "type": "object",
      "required": ["can_edit", "can_trigger_manual_session"],
      "properties": {
        "can_edit": { "type": "boolean" },
        "can_trigger_manual_session": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "recent_sessions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "status", "trigger", "baseline_analysis_id", "current_analysis_id", "current_commit_sha", "has_drift_report", "created_at", "started_at", "finished_at", "last_error"],
        "properties": {
          "id": { "type": "string" },
          "status": { "type": "string" },
          "trigger": { "type": "object" },
          "baseline_analysis_id": { "type": ["string", "null"] },
          "current_analysis_id": { "type": ["string", "null"] },
          "current_commit_sha": { "type": ["string", "null"] },
          "has_drift_report": { "type": "boolean" },
          "created_at": { "type": "string", "format": "date-time" },
          "started_at": { "type": ["string", "null"], "format": "date-time" },
          "finished_at": { "type": ["string", "null"], "format": "date-time" },
          "last_error": { "type": "string" }
        },
        "additionalProperties": false
      }
    },
    "recent_pr_plans": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "title", "blocked", "block_reason", "approval_status", "execution_status", "created_pr_url", "execution_error", "terminal_outcome", "terminal_outcome_at", "confidence", "confidence_threshold", "created_at"],
        "properties": {
          "id": { "type": "string" },
          "title": { "type": "string" },
          "blocked": { "type": "boolean" },
          "block_reason": { "type": ["string", "null"] },
          "approval_status": { "type": "string" },
          "execution_status": { "type": "string" },
          "created_pr_url": { "type": ["string", "null"] },
          "execution_error": { "type": ["string", "null"] },
          "terminal_outcome": { "type": ["string", "null"] },
          "terminal_outcome_at": { "type": ["string", "null"], "format": "date-time" },
          "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
          "confidence_threshold": { "type": "number", "minimum": 0, "maximum": 1 },
          "created_at": { "type": "string", "format": "date-time" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
